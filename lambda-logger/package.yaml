# This YAML file describes your package. Stack will automatically generate a
# Cabal file when you run `stack build`. See the hpack website for help with
# this file: <https://github.com/sol/hpack>.
category: Other
description: LDE logging infrastructure.

default-extensions:
  - OverloadedStrings

extra-source-files:
- CHANGELOG.md
- LICENSE.md
- package.yaml
- README.md
- stack.yaml
ghc-options: -Wall
library:
  dependencies:
    - base
    - monad-logger
    - fast-logger
    - interpolate
  source-dirs: library
license: MIT
maintainer: Yorick Laupa
name: lambda-logger
synopsis: A new Haskeleton package.
version: '0.0.0'
